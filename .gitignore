# Build output
dist/
.astro/

# Dependencies
node_modules/

# Résumé content + PDF — single-sourced from the Bachmann1234/resume repo,
# fetched at build time (CI) or from the sibling checkout (local dev).
resume.json
public/resume.pdf

# OS / editor cruft
.DS_Store

# Local preview / screenshot artifacts (card images live in public/assets/)
.playwright-mcp/
/*.png
