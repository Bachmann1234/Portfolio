# Résumé PDF is fetched from the Bachmann1234/resume repo at deploy time
resume.pdf

# Python (resume.json -> resume.html render step)
.venv/
__pycache__/

# OS / editor cruft
.DS_Store

# Playwright MCP scratch output
.playwright-mcp/

# Deploy build output / fetched résumé artifacts
dist/
resume.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
